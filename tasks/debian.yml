---
- name: install postfix-relay packages (apt)
  ansible.builtin.apt:
    name: "{{ postfix_relay_packages }}"
    state: "present"
    install_recommends: false
    force_apt_get: true
  tags: postfix_relay
