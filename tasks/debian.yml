---
# tasks file for roles/postfix-relay

- name: install postfix-relay packages (apt)
  apt:
    name: "{{ item }}"
    state: "latest"
  with_items:
    - postfix
    - postfix-pcre
    - make
    - dovecot-core
    - dovecot-sqlite
    - sqlite3
  tags: postfix-relay
